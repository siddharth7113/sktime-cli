# H01 — Forecasting tournament and holdout deployment

> Reviewer-only detail. Never place this file in model context.

## Why this is hard

The agent must manage a true holdout, run three comparable five-fold
backtests, interpret two metrics, select by an explicit policy, and deploy a
saved model without leaking the final 12 observations.

## Reference decision

| Candidate | Mean MAPE | Mean MAE |
|---|---:|---:|
| Naive sp=1 | 0.158267 | 62.733333 |
| Naive sp=12 | 0.108560 | 37.883333 |
| Polynomial degree=1 | 0.111614 | 43.022209 |

The champion is `NaiveForecaster(sp=12)`. Fitting the 132-row training set
gives cutoff 1959-12; its 12 predictions run from 1960-01 through 1960-12.

## Critical review points

- Every candidate must use exactly the same splitter and metrics.
- Neither selection nor champion fitting may use `airline_test.csv`.
- The model must not invent a final holdout error: this CLI cannot score an
  existing forecast file against an existing truth file.
- All requested model and forecast artifacts must be inspected.
